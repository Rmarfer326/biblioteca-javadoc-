<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Reglas CheckStyle del proyecto Biblioteca
    Ejecutar: mvn checkstyle:check
-->
<module name="Checker">

    <property name="charset" value="UTF-8"/>

    <!-- Sin tabuladores -->
    <module name="FileTabCharacter"/>

    <module name="TreeWalker">

        <!-- Imports sin usar -->
        <module name="UnusedImports"/>

        <!-- Líneas demasiado largas (máx. 100 caracteres) -->
        <module name="LineLength">
            <property name="max" value="100"/>
        </module>

        <!-- Nombres de constantes en MAYUSCULAS -->
        <module name="ConstantName"/>

        <!-- Nombres de variables en camelCase -->
        <module name="LocalVariableName"/>
        <module name="MemberName"/>

        <!-- Llaves siempre obligatorias en if/for/while -->
        <module name="NeedBraces"/>

        <!-- Bloques catch vacíos prohibidos -->
        <module name="EmptyCatchBlock"/>

        <!-- Javadoc en métodos públicos -->
        <module name="MissingJavadocMethod">
            <property name="scope" value="public"/>
        </module>

        <!-- Números mágicos (salvo -1, 0, 1, 2) -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
        </module>

        <!-- equals() debe tener hashCode() -->
        <module name="EqualsHashCode"/>

    </module>

</module>
